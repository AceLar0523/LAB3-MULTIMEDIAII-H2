import { Routes } from '@angular/router';
import { AvatarComponent } from './components/avatar/avatar'; // Importa el componente

export const routes: Routes = [
  { path: 'avatar', component: AvatarComponent } // Añade esta línea
];